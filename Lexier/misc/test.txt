! this is a comment for this example code which converts Fahrenheit to Celsius !

function convert$ [fahr:int]
{
	return 5 * (fahr - 32) / 9;
}

%%
	int low, high, step$;    ! declaration !
	
	get(low, high, step$);
	while(low < high)
	{ 	put(low);
		put(convert$(low));
		low = low + step$;
	}